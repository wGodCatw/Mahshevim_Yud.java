package JavaTutorialForBeginners.InterfaceGuide;

public interface Changeable { //interface contains methods without realisations

    //interface can have default methods with realisations, but I was too lazy to show that
    void change();
    int getter();
}
